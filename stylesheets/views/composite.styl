composite-view()
  .composite-view
    display: flex
    padding: 44px 0 0 0
    margin: 0 auto

new-composite-view(name, background=transparent, color=#fff)
  .composite-view-{name}
    background: background
    color: color
